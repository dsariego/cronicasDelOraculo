import { CommonsDirective } from './commons.directive';

describe('CommonsDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonsDirective();
    expect(directive).toBeTruthy();
  });
});
